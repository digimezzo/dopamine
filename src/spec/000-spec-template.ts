import * as assert from 'assert';
import { Times, It, Mock, IMock } from 'typemoq';

describe('Class', () => {
    describe('method', () => {
        it('Condition', () => {
            // Arrange

            // Act

            // Assert
        });
    });
});

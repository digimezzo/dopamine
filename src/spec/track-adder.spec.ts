import * as assert from 'assert';

describe('TrackAdder', () => {
    describe('addTracksThatAreNotInTheDatabase', () => {
        it('Should add tracks that are not in the database', () => {
            // Arrange

            // Act

            // Assert
            assert.fail();
        });
    });
});
